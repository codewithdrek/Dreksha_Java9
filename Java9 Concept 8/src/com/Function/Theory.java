package com.Function;

public class Theory {

	//Function 
	
	//R apply<T t,R r>{}
	//Return any value,it return any predicate(boolean) or any type of output
	
	//But Predicate always return only boolean
	

	//Default Method : Compose(),andThen()
	//Abstract Method: apply(T t , R r)
	//Static Method :  identify() -- Function identity(){} return type is same whatever input u are proving is same is return
	
	
} 
